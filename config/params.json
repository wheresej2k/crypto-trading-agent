{
  "_comment": "Tunable strategy/risk knobs. NOT secret - committed to git on purpose so the monthly auto-retune workflow can propose changes here as a reviewable pull request diff. See README.md for what each field means. short_sma_window/long_sma_window/take_profit_pct/min_confidence are an interim seed from tune.py's best pre-trend-filter result (2026-09-14); trend_window is a placeholder pending a sweep that includes it - do not treat this as validated until tune.py has actually tested this combination together.",
  "short_sma_window": 4,
  "long_sma_window": 24,
  "trend_window": 240,
  "stop_loss_pct": 18,
  "take_profit_pct": 8,
  "min_confidence": 60,
  "max_position_pct": 15,
  "max_total_exposure_pct": 75,
  "max_daily_loss_pct": 5,
  "max_trades_per_run": 3
}
